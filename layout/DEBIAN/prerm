#!/bin/bash
# TrollShot prerm
# 卸载本 deb 包时清理系统目录中的 daemon 和 plist。

set -e

launchctl unload -w /Library/LaunchDaemons/com.hogan.trollshot.plist 2>/dev/null || true
rm -f /Library/LaunchDaemons/com.hogan.trollshot.plist
rm -f /usr/bin/trollshotd
rm -rf /var/mobile/trollshot

exit 0
