name: 构建 TrollShot

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  FINALPACKAGE: 1
  HOMEBREW_NO_AUTO_UPDATE: 1
  HOMEBREW_NO_INSTALLED_DEPENDENTS_CHECK: 1

jobs:
  build:
    runs-on: macos-14

    steps:
      - name: 检出代码库
        uses: actions/checkout@v4

      - name: 安装依赖
        run: |
          brew install ldid-procursus p7zip make dpkg

      - name: 检出 Theos
        uses: actions/checkout@v4
        with:
          repository: theos/theos
          path: theos
          submodules: recursive

      - name: 安装 iOS SDK
        run: |
          export THEOS=$GITHUB_WORKSPACE/theos
          mkdir -p $THEOS/sdks
          git clone --depth=1 https://github.com/theos/sdks.git /tmp/theos-sdks
          cp -R /tmp/theos-sdks/iPhoneOS16.5.sdk $THEOS/sdks/
          cp -R /tmp/theos-sdks/iPhoneOS14.5.sdk $THEOS/sdks/
          ls -la $THEOS/sdks/

      - name: 构建应用
        run: |
          set -o pipefail
          export THEOS=$GITHUB_WORKSPACE/theos
          rm -rf .theos
          make clean package 2>&1 | tee build.log

          echo "=== 检查 staging .app 中的 daemon 和 plist ==="
          ls -la .theos/_/Applications/TrollShot.app/ || true

          echo "=== 验证 plist 路径 ==="
          plutil -p .theos/_/Library/LaunchDaemons/com.hogan.trollshot.plist || cat .theos/_/Library/LaunchDaemons/com.hogan.trollshot.plist || true

          echo "=== 检查 .deb 中的关键文件 ==="
          dpkg-deb -c packages/com.hoganhong.trollshot_*.deb | grep -E "trollshotd|\.plist" || true

          echo "=== 用 gzip 压缩重建 deb (rootful 兼容) ==="
          rm -f packages/*.deb
          dpkg-deb -Zgzip -b .theos/_ packages/com.hoganhong.trollshot_1.0.0_iphoneos-arm64.deb
          echo "=== 重建后的 deb 信息 ==="
          dpkg-deb -I packages/com.hoganhong.trollshot_1.0.0_iphoneos-arm64.deb
          dpkg-deb -c packages/com.hoganhong.trollshot_1.0.0_iphoneos-arm64.deb | grep -E "trollshotd|\.plist"

      - name: 打包 IPA
        run: |
          set -e
          # 验证 staging .app 中包含 Info.plist
          if [ ! -f ".theos/_/Applications/TrollShot.app/Info.plist" ]; then
            echo "❌ Info.plist 不在 .app bundle 中！手动复制..."
            cp -p Info.plist ".theos/_/Applications/TrollShot.app/Info.plist"
          fi
          # 从 staging 目录拿出 .app，按 TrollStore 标准打包成 Payload/*.app
          mkdir -p Payload
          cp -R .theos/_/Applications/TrollShot.app Payload/
          # 验证 Info.plist 已复制
          if [ ! -f "Payload/TrollShot.app/Info.plist" ]; then
            echo "❌ Payload/TrollShot.app/Info.plist 仍缺失！"
            exit 1
          fi
          echo "✅ Info.plist 验证通过"
          # TrollStore 安装时需要可执行权限
          chmod -R +x Payload/TrollShot.app
          zip -yqr TrollShot.ipa Payload
          rm -rf Payload
          ls -lh TrollShot.ipa
          # 列出 IPA 内容确认结构
          echo "=== IPA 内容 ==="
          python3 -c "import zipfile; [print(n) for n in zipfile.ZipFile('TrollShot.ipa').namelist()]"

      - name: 上传构建日志
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: build.log
          if-no-files-found: ignore

      - name: 上传 IPA 构建产物
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: TrollShot-ipa
          path: TrollShot.ipa
          if-no-files-found: error

      - name: 上传 deb 越狱包
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: TrollShot-deb
          path: packages/*.deb
          if-no-files-found: error

      - name: 上传解包调试信息
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: package-debug
          path: .theos/_
          if-no-files-found: ignore
