name: runner 诊断（临时，可删）
on: workflow_dispatch
jobs:
  test-ubuntu:
    runs-on: ubuntu-latest
    steps:
      - run: echo "ubuntu runner OK"
  test-macos:
    runs-on: macos-14
    steps:
      - run: echo "macos runner OK"
